const singleNumber = nums => {
    const map = new Map()
    for(const num of nums){
        if(map.has(num)) 
        else
    }
};

// Input: [2,2,3,2]
// Output: 3

console.log(singleNumber([2, 2, 3, 2]));
